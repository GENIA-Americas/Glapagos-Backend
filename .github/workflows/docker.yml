name: Docker Build

on:
  pull_request:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -f compose/production/django/Dockerfile -t glapagos-backend .
