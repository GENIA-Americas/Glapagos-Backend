{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "compose/production/django/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"if [ ${RUN_AS_WORKER:-false} = true ]; then celery -A config.celery_app worker -l INFO --concurrency=1; else echo $GOOGLE_CREDENTIALS_JSON_B64 | base64 -d > /tmp/gcp-key.json && gunicorn config.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120; fi\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
